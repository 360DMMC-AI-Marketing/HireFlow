// API calls for interview operations
import axios from 'axios';

const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000/api';

export const interviewApi = {
  // Add interview API methods here
};
